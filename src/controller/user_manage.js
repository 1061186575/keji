/***
 *  超级管理员用户管理
 */
layui.define(["element", "table", "layer", "form"], function(exports) {
  let element = layui.element;
  let table = layui.table;
  let layer = layui.layer;
  let form = layui.form;
  let $ = layui.jquery;
  let admin = layui.admin;
  let view = layui.view;
  form.render();

  // delete

  exports("user_manage", {});
});
